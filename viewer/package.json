{
  "name": "viewer",
  "version": "1.0.0",
  "private": true,
  "description": "Static DuckDB-Wasm dashboard for testlake CI data",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run"
  },
  "license": "MIT",
  "dependencies": {
    "@duckdb/duckdb-wasm": "^1.33.1-dev57.0",
    "uplot": "^1.6.32"
  },
  "devDependencies": {
    "@duckdb/node-api": "^1.5.4-r.1",
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vitest": "^4.1.10"
  }
}
